apiVersion: v1
kind: Pod
metadata:
  name: my-pv-pod
spec:
  containers:
    - name: my-contaier
      image: httpd
      ports:
        containerPort: 80
      volumeMounts:
        - mountPath: /mnt
          name: new-vol
  volumes:
    - name: new-vol
      presistantVolumeClaim:
        claimName: my-pvct